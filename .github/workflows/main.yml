name: My First Workflow

on:
  push:
  workflow_dispatch:

jobs:
  hello:
    runs-on: ubuntu-latest

    env:
      MY_NAME: Sridev

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: List repository files
        run: |
          echo "Files in my repository:"
          ls -la

      - name: Read README
        run: cat README.md

      - name: Print my name
        run: echo "My name is $MY_NAME"
